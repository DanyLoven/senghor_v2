import { GenerateCitationOfChallenge } from './generate-citation-of-challenge';

describe('GenerateCitationOfChallenge', () => {
  it('should create an instance', () => {
    expect(new GenerateCitationOfChallenge()).toBeTruthy();
  });
});
